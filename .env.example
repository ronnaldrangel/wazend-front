# =============================================================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# =============================================================================
# Copia este archivo como .env.local y completa los valores reales
# NUNCA subas .env.local al repositorio

# =============================================================================
# VARIABLES PÚBLICAS (Expuestas al cliente - prefijo NEXT_PUBLIC_)
# =============================================================================

# URL del backend/API
NEXT_PUBLIC_BACKEND_URL=https://tu-backend.com

# URLs de la aplicación
NEXT_PUBLIC_URL=https://tu-dominio.com
NEXT_PUBLIC_APP_NAME=Tu App

# Configuración de imágenes y assets
NEXT_PUBLIC_LOGO=/images/logo.svg
NEXT_PUBLIC_LOGO_DARK=/images/logo-dark.svg
NEXT_PUBLIC_AUTH_BG=/images/bg-auth.png

# WhatsApp
NEXT_PUBLIC_WHATSAPP_NUMBER=+1234567890

# URLs de Wazend API (solo la URL base, no el token)
NEXT_PUBLIC_WAZEND_API_URL=https://api.wazend.com

# WooCommerce Store URL
NEXT_PUBLIC_WC_STORE_URL=https://tu-tienda.com

# Webhooks públicos (solo URLs, no tokens)
NEXT_PUBLIC_CREATE_INSTANCE=https://webhook.com/create
NEXT_PUBLIC_DELETE_INSTANCE=https://webhook.com/delete
NEXT_PUBLIC_CREATE_TRIAL=https://webhook.com/trial
NEXT_PUBLIC_DELETE_TRIAL=https://webhook.com/trial/delete
NEXT_PUBLIC_UPDATE_GROUP=https://webhook.com/group
NEXT_PUBLIC_REGISTER=https://webhook.com/register

# =============================================================================
# VARIABLES PRIVADAS (Solo del servidor - SIN prefijo NEXT_PUBLIC_)
# =============================================================================

# NextAuth
NEXTAUTH_SECRET=tu-nextauth-secret-muy-seguro
NEXTAUTH_URL=http://localhost:3000

# OAuth Providers
GOOGLE_CLIENT_ID=tu-google-client-id
GOOGLE_CLIENT_SECRET=tu-google-client-secret
GITHUB_CLIENT_ID=tu-github-client-id
GITHUB_CLIENT_SECRET=tu-github-client-secret

# API Tokens (CRÍTICO: Mantener privados)
API_TOKEN=tu-api-token-super-secreto
WAZEND_API_KEY=tu-wazend-api-key-secreto

# WooCommerce (Credenciales privadas)
WC_CONSUMER_KEY=ck_tu-consumer-key
WC_CONSUMER_SECRET=cs_tu-consumer-secret

# =============================================================================
# NOTAS DE SEGURIDAD
# =============================================================================
# 1. Las variables con NEXT_PUBLIC_ son visibles en el cliente
# 2. Las variables sin NEXT_PUBLIC_ solo están disponibles en el servidor
# 3. NUNCA pongas tokens o secretos en variables NEXT_PUBLIC_
# 4. Usa API routes para manejar operaciones que requieren tokens privados
# 5. Siempre valida y sanitiza datos en el servidor
# =============================================================================